{
  "name": "synchronicity-engine-monorepo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "tsc -b",
    "clean": "rimraf apps/**/dist packages/**/dist"
  },
  "packageManager": "npm@10"
}
